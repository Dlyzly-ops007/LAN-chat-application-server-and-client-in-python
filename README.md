# LAN-chat-application-server-and-client-in-python
A simple local network chat application built in Python using socket and threading for server-client communication and tkinter for GUI. This project demonstrates basic networking and GUI programming by enabling multiple clients to chat with each other via a server on the same network.
